# Contact form endpoint. Create a free form at https://formspree.io
# and paste the endpoint URL here. Without this the form stays disabled
# and the page shows the fallback email instead.
VITE_CONTACT_ENDPOINT=

# Optional: shown in the drawing title block as the revision tag.
VITE_REVISION=A
